# GeoCharge Montréal — Dépendances de l'ancien backend Django (conservé pour référence, non utilisé)
#
# ⚠ L'application qui tourne réellement est shiny_app/app.py (Shiny for Python).
#   Pour l'installer : pip install -r shiny_app/requirements.txt
#   Ce fichier-ci liste les dépendances du backend Django + PostGIS remplacé le 14 juillet 2026
#   (voir shiny_app/app.py, docstring). Il n'est plus nécessaire pour faire tourner le projet.

# === Géospatial ===
geopandas==1.0.1
shapely==2.0.4
fiona==1.10.1
pyproj==3.7.1

# === Base de données ===
psycopg2-binary==2.9.12
sqlalchemy==2.0.41
geoalchemy2==0.20.0

# === Web / Django ===
django==5.2.15
djangorestframework==3.17.1
djangorestframework-gis==1.2.1
django-cors-headers==4.9.0
django-filter==25.2
asgiref==3.11.1
sqlparse==0.5.5
apscheduler==3.11.3

# === Production (Railway) ===
gunicorn==21.2.0
whitenoise==6.7.0

# === Utilitaires ===
numpy>=1.24
requests>=2.31
python-dotenv>=1.0
